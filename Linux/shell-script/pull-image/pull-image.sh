 for image in `cat multus-daemonset-thick-plugin.yml | grep -w image | awk -F " " '{print $2}'`;

 # cat multus-daemonset-thick-plugin.yml | grep -w image | awk -F " " '{print $2}'
 # execute this directly in git bash
